<div>
    <p class="section-title">{{title}}</p>
    <ul>
        {{#each content}}
            <li>{{skill}}
                <div>{{rating}}</div>
            </li>
        {{/each}}
    </ul>
</div>